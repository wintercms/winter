import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';

export default defineConfig({
    plugins: [
        laravel({
            input: [
                'assets/css/{{packageName}}.css',
                'assets/js/{{packageName}}.js',
            ],
            refresh: {
                paths: [
                    './**/*.htm',
                    './**/*.block',
                    'assets/**/*.css',
                    'assets/**/*.js',
                ]
            },
        }),
    ],
});
